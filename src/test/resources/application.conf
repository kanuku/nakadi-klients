akka {
 # loglevel = "DEBUG"
 # stdout-loglevel = "DEBUG"

  actor {
    debug {
      # enable DEBUG logging of actor lifecycle changes
      lifecycle = on
      unhandled = on
      autoreceive = on
    }
  }
}
